<p>クイックシェア</p>

<%= link_to 'メモ作成', memos_main_path, class: 'btn btn-primary' %>
<div class="modal fade" id="searchModal" tabindex="-1" role="dialog" aria-labelledby="searchModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <p class="modal-title" id="searchModalLabel">メモ検索</p>
      </div>
      <div class="modal-body">
        <%= form_with url: memos_main_path, method: :get do |f| %>
          <div class="form-group">
            <%= f.label :password, 'キーワード' %>
            <%= f.text_field :password, class: 'form-control' %>
          </div>
          <%= f.submit '検索', class: 'btn btn-primary' %>
          <%= button_to '削除', memos_main_path(p), method: :delete, data: { confirm: '本当に削除しますか？' } %>
        <% end %>
      </div>
    </div>
  </div>
</div>
